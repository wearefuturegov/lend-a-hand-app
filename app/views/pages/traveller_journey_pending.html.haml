%header.header
  .back
    = link_to page_path(id: 'traveller_journey_add'), :title => "Profile" do
      = fa_icon "long-arrow-left"
%main.main
  %section.container
    %section.body
      %section.card

        %h2
          = t('traveller_journey_pending.title')

      %section.key.card
        = render partial: 'partials/route_large'

      %section.actions.primary
        = render partial: "partials/button", locals: {one: I18n.t('button.cancel_journey'), myclass: 'button primary'}
  = render partial: 'partials/about_station'
